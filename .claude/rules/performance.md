# Performance Rules

- Keep active MCP servers and plugins to a minimum (max ~80 tools total)
- Use Sonnet for mechanical agents to save context and cost
- Reserve Opus for deep reasoning tasks only
- Run `/compact` to free context before heavy coding phases
- Read codemap for orientation instead of scanning the entire codebase
- Batch git operations where possible (debounced commits)
