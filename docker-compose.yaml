services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - /notes:/vault
      - ./config:/app/config
    environment:
      - TG_BOT_TOKEN
      - OPENROUTER_API_KEY
      - GITHUB_TOKEN

volumes:
  vault_repo:
